resources:
  pipelines:
    bronze_pipeline:
      name: bronze_ingestion_pipeline
      catalog: ${var.catalog_name}
      target: ${var.schema_name}
      serverless: true
      libraries:
        - notebook:
            path: ../src/bronze_table.py
        - notebook:
            path: ../src/silver_tables.sql
      continuous: false